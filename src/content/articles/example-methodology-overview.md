---
title: 'Our approach, in plain terms'
description: 'Placeholder article demonstrating the article frontmatter shape and prose styling — replace with real content.'
category: methodology
publishDate: 2026-08-29
draft: true
---

This is a placeholder article. Replace it with real content once it's ready — it exists so the article template, frontmatter schema, and `prose` typography styling have something real to render against while the site is being built.

## A subheading

Body copy renders through the `prose` classes defined in `global.css`, which are mapped onto this project's own color tokens (see `src/styles/tokens.css`) rather than Tailwind Typography's defaults — so long-form parent content always matches the rest of the site.
